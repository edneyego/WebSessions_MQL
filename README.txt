Install instructions. 

1. Git clone this project into a subdirectory in ./Scripts
2. Save the headers text file (in this path) to ./Files and rename to "headers.txt"
3. Click/load the MQL project (WebSession.mqproj)
4. Compile and run unit tests. WebSession.mq5 (script)